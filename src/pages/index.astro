---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import type { CollectionEntry } from 'astro:content';

type Proyecto = CollectionEntry<'proyectos'>;

// Proyectos destacados para el home
const proyectos = await getCollection('proyectos');
const destacados = proyectos
  .filter((p: Proyecto) => p.data.destacado)
  .sort((a: Proyecto, b: Proyecto) => (a.data.orden ?? 99) - (b.data.orden ?? 99))
  .slice(0, 3);
---

<BaseLayout
  title="INACONS | Empresa Constructora en Perú"
  description="Empresa constructora peruana con más de 15 años en obras civiles, infraestructura, electromecánica y consultoría a nivel nacional."
>
  <!-- Preload LCP — solo en home donde esta imagen es el elemento visual principal -->
  <link slot="head" rel="preload" as="image" href="/assets/imagenes/image_heroprincipal.webp" fetchpriority="high" />

  <style slot="head">
    /* ── HERO ── */
    .home-hero {
      position: relative;
      width: 100%;
      height: 100vh;
      min-height: 620px;
      display: flex;
      align-items: center;
      overflow: hidden;
    }
    .hero-video {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      z-index: 0;
    }
    .hero-overlay {
      position: absolute;
      inset: 0;
      z-index: 1;
      background:
        linear-gradient(to bottom, rgba(20,23,45,0.30) 0%, rgba(20,23,45,0.92) 100%),
        linear-gradient(110deg, rgba(20,23,45,0.85) 0%, rgba(27,82,120,0.35) 60%, transparent 100%);
    }
    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 1200px;
      width: 100%;
      margin: 0 auto;
      padding: 0 40px;
    }
    .home-hero h1 {
      font-family: 'Montserrat', sans-serif;
      font-size: clamp(2.2rem, 5vw, 3.8rem);
      font-weight: 700;
      text-transform: uppercase;
      line-height: 1.1;
      margin-bottom: 28px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      letter-spacing: -0.6px;
    }
    .hero-line {
      display: flex;
      align-items: baseline;
      gap: 14px;
      flex-wrap: wrap;
    }
    .word.fixed   { color: rgba(255,255,255,0.68); font-weight: 300; letter-spacing: 1.5px; }
    .word.highlight { color: #fff; font-weight: 800; letter-spacing: -0.5px; }
    .word.highlight.completed {
      background: linear-gradient(transparent 84%, rgba(27,82,120,0.40) 84%);
    }
    /* Ghost reserva el espacio final desde el inicio — sin CLS */
    .word.typewriter {
      position: relative;
      display: inline-block;
    }
    .tw-ghost {
      visibility: hidden;
      user-select: none;
      pointer-events: none;
    }
    .tw-typed {
      position: absolute;
      left: 0;
      top: 0;
      white-space: nowrap;
      color: inherit;
    }
    .word.typewriter.typing .tw-typed::after {
      content: '|';
      color: rgba(255,255,255,0.55);
      animation: blink .5s infinite;
      margin-left: 2px;
    }
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50%       { opacity: 0; }
    }
    .hero-content > p {
      font-size: clamp(0.95rem, 1.6vw, 1.1rem);
      color: rgba(255,255,255,0.68);
      margin-bottom: 36px;
      max-width: 500px;
      font-weight: 300;
      letter-spacing: 0.2px;
    }
    .hero-actions { display: flex; gap: 12px; flex-wrap: wrap; }

    /* ── INDICADORES ── */
    .hero-indicators {
      position: absolute;
      bottom: 0; left: 0; right: 0;
      z-index: 3;
      background: var(--c-primary);
      border-top: 1px solid rgba(27,82,120,0.5);
    }
    .indicators-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 40px;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
    }
    .indicator {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 26px 16px;
      border-right: 1px solid rgba(255,255,255,0.07);
      position: relative;
      overflow: hidden;
      transition: background .28s ease;
    }
    .indicator::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      width: 0;
      height: 2px;
      background: var(--c-accent);
      transition: width .35s ease;
    }
    .indicator:hover::after { width: 52%; }
    .indicator:last-child { border-right: none; }
    .indicator:hover { background: rgba(255,255,255,0.04); }
    .indicator-icon { color: rgba(255,255,255,0.28); margin-bottom: 10px; }
    .indicator-number {
      font-size: clamp(2rem, 4vw, 2.8rem);
      font-weight: 800;
      color: #fff;
      line-height: 1;
      letter-spacing: -1.5px;
    }
    .indicator-text {
      font-size: 0.75rem;
      color: rgba(255,255,255,0.55);
      text-transform: uppercase;
      letter-spacing: 0.8px;
      margin-top: 4px;
      text-align: center;
    }

    /* ── ABOUT (Bechtel-style 2-col) ── */
    .about-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 88px;
      align-items: center;
    }
    .about-img-frame {
      position: relative;
      height: 540px;
    }
    .about-img-frame::before {
      content: '';
      position: absolute;
      top: -14px; left: -14px; right: 14px; bottom: 14px;
      border: 2px solid rgba(217,130,43,0.40);
      z-index: 0;
    }
    .about-img-frame img {
      position: relative;
      z-index: 1;
      width: 100%; height: 100%;
      object-fit: cover;
      object-position: center 20%;
    }
    .about-years-badge {
      position: absolute;
      bottom: -18px; right: -18px;
      z-index: 2;
      background: var(--c-primary);
      padding: 20px 24px;
      min-width: 112px;
      text-align: center;
    }
    .badge-num {
      display: block;
      font-family: 'Montserrat', sans-serif;
      font-size: 2.4rem;
      font-weight: 800;
      letter-spacing: -2px;
      line-height: 1;
      color: var(--c-accent);
    }
    .badge-text {
      display: block;
      font-size: 0.62rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: rgba(255,255,255,0.55);
      margin-top: 6px;
      line-height: 1.45;
    }
    .about-content { display: flex; flex-direction: column; }
    .about-content .sh-label { margin-bottom: 14px; }
    .about-heading {
      font-family: 'Montserrat', sans-serif;
      font-size: clamp(1.75rem, 2.8vw, 2.4rem);
      font-weight: 700;
      color: var(--c-primary);
      line-height: 1.18;
      margin: 0 0 20px;
    }
    .about-divider {
      width: 44px; height: 3px;
      background: var(--c-accent);
      margin-bottom: 22px;
    }
    .about-body-text {
      font-size: 0.975rem;
      color: rgba(20,23,45,0.68);
      line-height: 1.78;
      margin: 0 0 14px;
    }
    .about-kpi-row {
      display: flex;
      border-top: 1px solid rgba(20,23,45,0.09);
      border-bottom: 1px solid rgba(20,23,45,0.09);
      padding: 22px 0;
      margin: 22px 0 32px;
    }
    .about-kpi {
      flex: 1;
      padding-right: 24px;
      border-right: 1px solid rgba(20,23,45,0.09);
    }
    .about-kpi:last-child { border-right: none; padding-right: 0; padding-left: 24px; }
    .about-kpi:first-child { padding-left: 0; }
    .about-kpi-num {
      display: block;
      font-family: 'Montserrat', sans-serif;
      font-size: clamp(1.6rem, 2.4vw, 2rem);
      font-weight: 800;
      color: var(--c-primary);
      letter-spacing: -1px;
      line-height: 1;
    }
    .about-kpi-label {
      display: block;
      font-size: 0.65rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      color: rgba(20,23,45,0.42);
      margin-top: 5px;
    }

    /* ── REGIONAL GRID ── */
    .regional-grid {
      display: grid;
      grid-template-columns: 1fr 1.5fr;
      gap: 80px;
      align-items: center;
    }
    .regional-map-wrap { display: flex; justify-content: center; }
    .peru-map {
      max-width: 260px;
      width: 100%;
      filter: drop-shadow(0 6px 20px rgba(20,23,45,0.10));
    }

    /* ── INFO CAROUSEL ── */
    .info-slide-inner {
      display: flex;
      flex-direction: column;
      height: 480px;
      border-radius: 0 !important;
      border: none !important;
      box-shadow: 0 4px 24px rgba(20,23,45,0.08) !important;
    }
    .info-slide-image {
      width: 100%;
      height: 260px;
      overflow: hidden;
      flex-shrink: 0;
      position: relative;
    }
    .info-slide-image img {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .info-slide-content {
      flex: 1;
      padding: 40px;
      display: flex;
      flex-direction: column;
      gap: 16px;
      justify-content: center;
      background: var(--c-primary) !important;
      position: relative;
    }
    .info-slide-content::before {
      content: '';
      position: absolute;
      top: 0; left: 0;
      width: 3px; height: 100%;
      background: var(--c-secondary);
    }
    .info-carousel-title {
      font-size: clamp(1.5rem, 2.5vw, 2rem);
      font-weight: 700;
      line-height: 1.2;
      margin: 0;
      color: #fff !important;
    }
    .info-carousel-description {
      font-size: 0.975rem;
      line-height: 1.75;
      margin: 0;
      color: rgba(255,255,255,0.68) !important;
    }
    .info-carousel-swiper .swiper-button-next,
    .info-carousel-swiper .swiper-button-prev {
      background: rgba(255,255,255,0.10) !important;
      color: #fff !important;
    }
    .info-carousel-swiper .swiper-button-next:hover,
    .info-carousel-swiper .swiper-button-prev:hover {
      background: var(--c-secondary) !important;
    }
    .info-carousel-swiper .swiper-pagination-bullet {
      background: rgba(255,255,255,0.30) !important;
      opacity: 1 !important;
    }
    .info-carousel-swiper .swiper-pagination-bullet-active {
      background: #fff !important;
    }

    /* Desktop — lado a lado */
    @media (min-width: 769px) {
      .info-slide-inner {
        display: grid;
        grid-template-columns: 1fr 1fr;
        height: 480px;
      }
      .info-slide-image {
        height: 100%;
      }
      .info-slide-content {
        padding: 40px;
      }
    }

    /* ── CLIENTES SCROLL ── */
    .clients-track-wrap {
      overflow: hidden;
      mask-image: linear-gradient(90deg, transparent, #000 7%, #000 93%, transparent);
      -webkit-mask-image: linear-gradient(90deg, transparent, #000 7%, #000 93%, transparent);
    }
    .clients-track {
      display: flex;
      width: max-content;
      animation: clientsScroll 30s linear infinite;
    }
    .clients-track:hover { animation-play-state: paused; }
    @keyframes clientsScroll {
      from { transform: translateX(0); }
      to   { transform: translateX(-50%); }
    }
    .client-logo-item {
      flex: 0 0 158px;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 18px 22px;
      border-right: 1px solid var(--c-border-light);
      filter: grayscale(100%);
      opacity: 0.48;
      transition: filter .24s ease, opacity .24s ease;
    }
    .client-logo-item:hover { filter: grayscale(0%); opacity: 1; }
    .client-logo-item img { max-height: 40px; max-width: 108px; object-fit: contain; }

    /* ── CTA BAND ── */
    .cta-band { background: var(--c-primary); padding: 88px 0; }
    .cta-band .sh-label { color: rgba(255,255,255,0.70); }
    .cta-band .sh-label::before,
    .cta-band .sh-label::after { background: rgba(255,255,255,0.25); }
    .cta-band .sh-title { color: #fff; }
    .cta-band .sh-desc  { color: rgba(255,255,255,0.62); }

    /* ── CTA ACTIONS ── */
    .cta-actions {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
      justify-content: center;
      margin-top: 40px;
    }

    /* ── EYEBROW ── */
    .hero-eyebrow {
      font-size: 0.70rem;
      font-weight: 600;
      letter-spacing: 2.5px;
      text-transform: uppercase;
      color: var(--c-accent);
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .hero-eyebrow::before {
      content: '';
      display: inline-block;
      width: 28px;
      height: 1.5px;
      background: var(--c-accent);
      flex-shrink: 0;
    }

    /* ── SCROLL HINT ── */
    .scroll-hint {
      position: absolute;
      bottom: 168px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 4;
      color: rgba(255,255,255,0.55);
      animation: scrollBounce 1.8s ease-in-out infinite;
      cursor: pointer;
    }
    .scroll-hint svg { display: block; }
    @keyframes scrollBounce {
      0%, 100% { transform: translateX(-50%) translateY(0);   opacity: 0.55; }
      50%       { transform: translateX(-50%) translateY(8px); opacity: 0.90; }
    }

    /* ── WORD CYCLE ── */
    .hero-cycle {
      position: relative;
      display: inline-block;
      overflow: hidden;
      vertical-align: bottom;
    }
    .cycle-ghost {
      visibility: hidden;
      user-select: none;
      pointer-events: none;
      font-weight: 800;
      letter-spacing: -0.5px;
    }
    .cycle-word {
      position: absolute;
      left: 0;
      top: 0;
      color: #fff;
      font-weight: 800;
      letter-spacing: -0.5px;
      white-space: nowrap;
      opacity: 0;
      transform: translateY(70%);
      animation: cycleWord 6s ease-in-out infinite;
    }
    .cycle-word:nth-child(2) { animation-delay: -0.5s; }
    .cycle-word:nth-child(3) { animation-delay:  1.5s; }
    .cycle-word:nth-child(4) { animation-delay:  3.5s; }
    @keyframes cycleWord {
      0%           { opacity: 0; transform: translateY(70%);  animation-timing-function: ease-out; }
      8.33%        { opacity: 1; transform: translateY(0);    animation-timing-function: ease;     }
      25%          { opacity: 1; transform: translateY(0);    animation-timing-function: ease-in;  }
      33.33%       { opacity: 0; transform: translateY(-55%); animation-timing-function: step-start; }
      33.34%, 100% { opacity: 0; transform: translateY(70%);  }
    }

    /* ── PAUSA VIDEO ── */
    .hero-pause-btn {
      position: absolute;
      right: 40px;
      bottom: 168px;
      z-index: 4;
      width: 36px;
      height: 36px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(255,255,255,0.10);
      border: 1px solid rgba(255,255,255,0.18);
      border-radius: 50%;
      color: rgba(255,255,255,0.65);
      cursor: pointer;
      transition: background var(--t), color var(--t), border-color var(--t);
    }
    .hero-pause-btn:hover {
      background: rgba(255,255,255,0.22);
      color: #fff;
      border-color: rgba(255,255,255,0.40);
    }
    /* icon-play initial state se maneja por JS en initHeroPause */

    /* Accesibilidad: ocultar video y mostrar imagen estática para usuarios con preferencia de movimiento reducido */
    @media (prefers-reduced-motion: reduce) {
      .hero-video { display: none; }
      .home-hero {
        background-image: url('/assets/imagenes/image_heroprincipal.webp');
        background-size: cover;
        background-position: center;
      }
      .cycle-word           { animation: none; opacity: 0; }
      .cycle-word:nth-child(2) { opacity: 1; transform: translateY(0); }
      .scroll-hint          { animation: none; opacity: 0.55; }
    }

    /* ── RESPONSIVE ── */
    @media (max-width: 768px) {
      /* Hero */
      .home-hero {
        min-height: 100svh;
        align-items: center;
      }
      .hero-eyebrow   { display: none; }
      .scroll-hint    { display: none; }
      .hero-pause-btn { display: none; }
      .hero-content {
        padding: 80px 20px 160px;
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      .home-hero h1 {
        text-align: center;
        font-size: clamp(1.35rem, 6.5vw, 1.9rem);
        width: 100%;
        padding: 0 4px;
        letter-spacing: -0.3px;
      }
      .hero-line {
        flex-wrap: nowrap;
        justify-content: center;
        gap: 8px;
        width: 100%;
        overflow: hidden;
      }
      .word.fixed,
      .word.highlight {
        white-space: nowrap;
        font-size: inherit;
      }
      .hero-content > p {
        text-align: center;
        margin-left: auto;
        margin-right: auto;
        max-width: 100%;
      }
      .hero-actions {
        justify-content: center;
        flex-direction: column;
        width: 100%;
      }
      .hero-actions .btn {
        width: 100%;
        justify-content: center;
      }

      /* About */
      .about-grid { grid-template-columns: 1fr; gap: 36px; }
      .about-img-wrap { height: 280px; }
      .about-img-wrap::before { display: none; }

      /* Regional */
      .regional-grid { grid-template-columns: 1fr; gap: 36px; }

      /* Indicadores */
      .indicators-container { grid-template-columns: repeat(2, 1fr); }

      /* Carrusel */
      .info-slide-inner {
        height: auto;
        min-height: 0;
      }
      .info-slide-image {
        height: 200px;
      }
      .info-slide-content {
        padding: 28px 24px;
      }
      .info-carousel-title {
        font-size: 1.3rem;
      }
      .info-carousel-description {
        font-size: 0.875rem;
      }
      .info-carousel-swiper .swiper-button-next,
      .info-carousel-swiper .swiper-button-prev {
        top: 110px;
      }

      /* CTA */
      .cta-actions {
        flex-direction: column;
        align-items: center;
        width: 100%;
      }
      .cta-actions .btn {
        width: 100%;
        max-width: 320px;
        justify-content: center;
      }
    }

    /* ── SERVICES SHOWCASE ── */
    .services-showcase {
      background: var(--c-primary);
      padding: 80px 0 0;
    }
    .services-showcase .sh-label        { color: rgba(255,255,255,0.55); }
    .services-showcase .sh-label::before,
    .services-showcase .sh-label::after  { background: rgba(255,255,255,0.20); }
    .services-showcase .sh-title         { color: #fff; }
    .services-showcase .sh-desc          { color: rgba(255,255,255,0.58); }
    .services-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2px;
      margin-top: 56px;
      background: rgba(255,255,255,0.07);
    }
    .service-card {
      position: relative;
      overflow: hidden;
      aspect-ratio: 4/3;
      display: block;
      text-decoration: none;
    }
    .service-card img {
      position: absolute;
      inset: 0;
      width: 100%; height: 100%;
      object-fit: cover;
      transition: transform 0.65s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    .service-card:hover img { transform: scale(1.07); }
    .svc-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(
        to top,
        rgba(20,23,45,0.93) 0%,
        rgba(20,23,45,0.42) 45%,
        rgba(20,23,45,0.12) 100%
      );
      transition: background 0.4s ease;
      z-index: 1;
    }
    .service-card:hover .svc-overlay {
      background: linear-gradient(
        to top,
        rgba(20,23,45,0.97) 0%,
        rgba(20,23,45,0.62) 50%,
        rgba(20,23,45,0.22) 100%
      );
    }
    .svc-body {
      position: absolute;
      bottom: 0; left: 0; right: 0;
      z-index: 2;
      padding: 28px 32px;
    }
    .svc-icon {
      color: var(--c-accent);
      margin-bottom: 10px;
      opacity: 0.88;
      transition: opacity 0.3s, transform 0.3s;
    }
    .service-card:hover .svc-icon { opacity: 1; transform: translateY(-2px); }
    .svc-title {
      font-size: 1.05rem;
      font-weight: 700;
      color: #fff;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin: 0 0 6px;
    }
    .svc-desc {
      font-size: 0.80rem;
      color: rgba(255,255,255,0.72);
      line-height: 1.55;
      margin: 0;
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      transition: max-height 0.4s ease, opacity 0.35s ease;
    }
    .service-card:hover .svc-desc { max-height: 64px; opacity: 1; }
    .svc-arrow {
      display: inline-flex;
      align-items: center;
      gap: 5px;
      margin-top: 10px;
      font-size: 0.70rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1.2px;
      color: var(--c-accent);
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      transition: max-height 0.35s ease 0.05s, opacity 0.35s ease 0.05s;
    }
    .service-card:hover .svc-arrow { max-height: 30px; opacity: 1; }

    /* ── PROYECTOS DESTACADOS ── */
    .projects-showcase {
      background: var(--c-primary);
      padding: 88px 0 0;
    }
    .projects-showcase .sh-label { color: var(--c-accent); opacity: 1; }
    .projects-showcase .sh-title { color: #fff; }
    .projects-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 3px;
      margin-top: 52px;
    }
    .project-card {
      position: relative;
      height: 500px;
      overflow: hidden;
      display: block;
      text-decoration: none;
    }
    .project-card img {
      width: 100%; height: 100%;
      object-fit: cover;
      transition: transform 0.85s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }
    .project-card:hover img { transform: scale(1.07); }
    .project-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(to top,
        rgba(20,23,45,0.90) 0%,
        rgba(20,23,45,0.28) 45%,
        rgba(20,23,45,0.05) 100%
      );
      transition: background 0.4s ease;
    }
    .project-card:hover .project-overlay {
      background: linear-gradient(to top,
        rgba(20,23,45,0.96) 0%,
        rgba(20,23,45,0.55) 52%,
        rgba(20,23,45,0.15) 100%
      );
    }
    .project-info {
      position: absolute;
      bottom: 0; left: 0; right: 0;
      padding: 30px 28px;
    }
    .project-tag {
      display: inline-block;
      font-size: 0.58rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1.6px;
      color: var(--c-accent);
      border: 1px solid rgba(217,130,43,0.45);
      padding: 3px 10px;
      margin-bottom: 10px;
    }
    .project-name {
      font-family: 'Montserrat', sans-serif;
      font-size: clamp(0.95rem, 1.3vw, 1.15rem);
      font-weight: 700;
      color: #fff;
      margin: 0 0 6px;
      line-height: 1.25;
    }
    .project-location {
      font-size: 0.70rem;
      color: rgba(255,255,255,0.48);
      margin: 0 0 14px;
    }
    .project-cta {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 0.68rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1.2px;
      color: var(--c-accent);
      opacity: 0;
      transform: translateY(8px);
      transition: opacity 0.3s ease, transform 0.3s ease;
    }
    .project-card:hover .project-cta { opacity: 1; transform: translateY(0); }
    .projects-footer { text-align: center; padding: 52px 0; }

    /* ── COVERAGE MAP ── */
    .coverage-stats {
      display: flex;
      justify-content: center;
      gap: 72px;
      margin: 40px 0 52px;
      padding: 28px 0;
      border-top: 1px solid rgba(20,23,45,0.08);
      border-bottom: 1px solid rgba(20,23,45,0.08);
    }
    .cov-stat { text-align: center; }
    .cov-stat-num {
      display: block;
      font-size: clamp(2rem, 4vw, 2.8rem);
      font-weight: 800;
      color: var(--c-primary);
      letter-spacing: -1px;
      line-height: 1;
    }
    .cov-stat-label {
      display: block;
      font-size: 0.70rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1.2px;
      color: rgba(20,23,45,0.45);
      margin-top: 7px;
    }
    .coverage-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 64px;
      align-items: center;
    }
    /* Mapa SVG interactivo por departamentos */
    .map-wrap {
      display: flex;
      justify-content: center;
      align-items: center;
    }
    #peruMapContainer {
      width: 100%;
      max-width: 280px;
    }
    /* El SVG se inyecta vía JS — estilos de departamentos aplicados inline en main.js */
    #peruMapContainer svg {
      width: 100%;
      height: auto;
      display: block;
      filter: drop-shadow(0 8px 28px rgba(20,23,45,0.12));
    }
    .map-stats { }
    .map-stats-title {
      font-size: 0.72rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 1.2px;
      color: rgba(20,23,45,0.40);
      margin-bottom: 28px;
    }
    .map-stat { margin-bottom: 18px; }
    .map-stat:last-child { margin-bottom: 0; }
    .map-stat-meta {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 7px;
    }
    .map-stat-city {
      font-size: 0.92rem;
      font-weight: 600;
      color: var(--c-primary);
    }
    .map-stat-count {
      font-size: 0.98rem;
      font-weight: 800;
      color: var(--c-accent);
      letter-spacing: -0.5px;
    }
    .map-stat-bar {
      height: 4px;
      background: rgba(20,23,45,0.07);
      border-radius: 2px;
      overflow: hidden;
    }
    .map-stat-fill {
      height: 100%;
      width: 0;
      background: linear-gradient(90deg, var(--c-secondary), var(--c-accent));
      border-radius: 2px;
      transition: width 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    }

    @media (max-width: 768px) {
      .services-showcase { padding: 60px 0 0; }
      .services-grid { grid-template-columns: repeat(2, 1fr); margin-top: 36px; }
      .service-card { aspect-ratio: 3/2; }
      .svc-body { padding: 14px 18px; }
      .svc-title { font-size: 0.82rem; }
      .svc-desc, .svc-arrow { display: none; }
      .about-grid { grid-template-columns: 1fr; gap: 48px; }
      .about-img-frame { height: 360px; }
      .about-years-badge { bottom: -12px; right: -12px; padding: 14px 18px; }
      .about-kpi-row { gap: 0; }
      .about-kpi { padding: 14px 16px; border-right: none; border-bottom: 1px solid rgba(20,23,45,0.09); }
      .about-kpi:last-child { border-bottom: none; padding-left: 16px; }
      .projects-grid { grid-template-columns: 1fr; }
      .project-card { height: 380px; }
      .project-cta { opacity: 1; transform: translateY(0); }
      .coverage-stats { gap: 28px; }
      .coverage-grid { grid-template-columns: 1fr; gap: 36px; }
      #peruMapContainer { max-width: 220px; }
    }
  </style>

  <!-- ① HERO -->
  <section class="home-hero">
    <video class="hero-video" autoplay muted loop playsinline poster="/assets/imagenes/image_heroprincipal.webp">
      <source src="/assets/videos/hero.mp4" type="video/mp4" />
    </video>
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <p class="hero-eyebrow">Ingeniería &amp; Construcción</p>
      <h1 id="typewriter-text">
        <span class="hero-line">
          <span class="word fixed">CONSTRUIMOS</span>
          <span class="word highlight typewriter" data-text="CONFIANZA" aria-label="CONFIANZA"><span class="tw-ghost" aria-hidden="true">CONFIANZA</span><span class="tw-typed" aria-hidden="true"></span></span>
        </span>
        <span class="hero-line">
          <span class="word highlight typewriter" data-text="INNOVAMOS" aria-label="INNOVAMOS"><span class="tw-ghost" aria-hidden="true">INNOVAMOS</span><span class="tw-typed" aria-hidden="true"></span></span>
          <span class="word fixed">ESPACIOS</span>
        </span>
        <span class="hero-line">
          <span class="word fixed">TRASCENDEMOS</span>
          <span class="word highlight typewriter" data-text="JUNTOS" aria-label="JUNTOS"><span class="tw-ghost" aria-hidden="true">JUNTOS</span><span class="tw-typed" aria-hidden="true"></span></span>
        </span>
      </h1>
      <p>Más de 15 años de experiencia en ingeniería y construcción al servicio del Perú.</p>
      <div class="hero-actions">
        <a href="/proyectos" class="btn btn-primary btn-lg">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m6 14 1.5-2.9A2 2 0 0 1 9.24 10H20a2 2 0 0 1 1.94 2.5l-1.54 6a2 2 0 0 1-1.95 1.5H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h3.9a2 2 0 0 1 1.69.9l.81 1.2a2 2 0 0 0 1.67.9H18a2 2 0 0 1 2 2v2"/></svg> Ver Proyectos
        </a>
        <a href="/assets/documentos/brochure_inacons.pdf" target="_blank" rel="noopener noreferrer" class="btn btn-outline-light btn-lg">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg> Descargar Brochure
        </a>
      </div>
    </div>
    <button class="hero-pause-btn" id="heroPauseBtn" aria-label="Pausar video">
      <svg class="icon-pause" width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><rect x="6" y="4" width="4" height="16" rx="1"/><rect x="14" y="4" width="4" height="16" rx="1"/></svg>
      <svg class="icon-play" width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><polygon points="5 3 19 12 5 21 5 3"/></svg>
    </button>
    <div class="scroll-hint" aria-hidden="true">
      <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="m6 9 6 6 6-6"/></svg>
    </div>
    <div class="hero-indicators">
      <div class="indicators-container">
        <div class="indicator">
          <div class="indicator-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg></div>
          <div class="indicator-number" data-target="15" data-suffix="+">0</div>
          <div class="indicator-text">Años de experiencia</div>
        </div>
        <div class="indicator">
          <div class="indicator-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="3 6 9 3 15 6 21 3 21 18 15 21 9 18 3 21"/><line x1="9" x2="9" y1="3" y2="18"/><line x1="15" x2="15" y1="6" y2="21"/></svg></div>
          <div class="indicator-number" data-target="150" data-suffix="+">0</div>
          <div class="indicator-text">km² construidos</div>
        </div>
        <div class="indicator">
          <div class="indicator-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="22 7 13.5 15.5 8.5 10.5 2 17"/><polyline points="16 7 22 7 22 13"/></svg></div>
          <div class="indicator-number" data-target="100" data-suffix="M+">0</div>
          <div class="indicator-text">Inversión gestionada</div>
        </div>
        <div class="indicator">
          <div class="indicator-icon"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg></div>
          <div class="indicator-number" data-target="100" data-suffix="+">0</div>
          <div class="indicator-text">Profesionales</div>
        </div>
      </div>
    </div>
  </section>

  <!-- ② SERVICIOS -->
  <section class="services-showcase">
    <div class="container" data-aos="fade-up" data-aos-duration="700">
      <div class="sh sh--center sh--medium">
        <p class="sh-label">Nuestras Especialidades</p>
        <h2 class="sh-title">Lo que construimos</h2>
        <p class="sh-desc">Seis líneas de servicio integradas para proyectos de cualquier escala, en cualquier región del Perú.</p>
      </div>
    </div>
    <div class="services-grid">
      <a href="/servicios/obras-civiles" class="service-card">
        <img src="/assets/imagenes/image_obra_civiles.webp" alt="Obras Civiles INACONS" loading="lazy" width="640" height="480">
        <div class="svc-overlay"></div>
        <div class="svc-body">
          <div class="svc-icon"><svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg></div>
          <h3 class="svc-title">Obras Civiles</h3>
          <p class="svc-desc">Infraestructura urbana, vial e industrial con metodología BIM</p>
          <span class="svc-arrow">Ver más <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg></span>
        </div>
      </a>
      <a href="/servicios/infraestructura" class="service-card">
        <img src="/assets/imagenes/image_infraestructura.webp" alt="Infraestructura INACONS" loading="lazy" width="640" height="480">
        <div class="svc-overlay"></div>
        <div class="svc-body">
          <div class="svc-icon"><svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect width="20" height="8" x="2" y="2" rx="2"/><rect width="20" height="8" x="2" y="14" rx="2"/><line x1="6" x2="6.01" y1="6" y2="6"/><line x1="6" x2="6.01" y1="18" y2="18"/></svg></div>
          <h3 class="svc-title">Infraestructura</h3>
          <p class="svc-desc">Proyectos de infraestructura pública y privada a nivel nacional</p>
          <span class="svc-arrow">Ver más <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg></span>
        </div>
      </a>
      <a href="/servicios/electromecanica" class="service-card">
        <img src="/assets/imagenes/image_electromecanica.webp" alt="Electromecánica INACONS" loading="lazy" width="640" height="480">
        <div class="svc-overlay"></div>
        <div class="svc-body">
          <div class="svc-icon"><svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg></div>
          <h3 class="svc-title">Electromecánica</h3>
          <p class="svc-desc">Instalaciones eléctricas y mecánicas para industria, minería e infraestructura</p>
          <span class="svc-arrow">Ver más <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg></span>
        </div>
      </a>
      <a href="/servicios/paisajismo" class="service-card">
        <img src="/assets/imagenes/image_paisajismo.webp" alt="Paisajismo INACONS" loading="lazy" width="640" height="480">
        <div class="svc-overlay"></div>
        <div class="svc-body">
          <div class="svc-icon"><svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M11 20A7 7 0 0 1 9.8 6.1C15.5 5 17 4.48 19 2c1 2 2 4.18 2 8 0 5.5-4.78 10-10 10z"/><path d="M2 21c0-3 1.85-5.36 5.08-6C9.5 14.52 12 13 13 12"/></svg></div>
          <h3 class="svc-title">Paisajismo</h3>
          <p class="svc-desc">Espacios verdes, parques y áreas exteriores que integran naturaleza y función</p>
          <span class="svc-arrow">Ver más <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg></span>
        </div>
      </a>
      <a href="/servicios/mineria" class="service-card">
        <img src="/assets/imagenes/image_mineria.webp" alt="Minería INACONS" loading="lazy" width="640" height="480">
        <div class="svc-overlay"></div>
        <div class="svc-body">
          <div class="svc-icon"><svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 3h12l4 6-10 13L2 9Z"/><path d="M11 3 8 9l4 13 4-13-3-6"/><path d="M2 9h20"/></svg></div>
          <h3 class="svc-title">Minería</h3>
          <p class="svc-desc">Ingeniería y construcción para el sector minero con los más altos estándares de seguridad</p>
          <span class="svc-arrow">Ver más <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg></span>
        </div>
      </a>
      <a href="/servicios/consultoria" class="service-card">
        <img src="/assets/imagenes/image_consultor.webp" alt="Consultoría INACONS" loading="lazy" width="640" height="480">
        <div class="svc-overlay"></div>
        <div class="svc-body">
          <div class="svc-icon"><svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="18" x2="18" y1="20" y2="10"/><line x1="12" x2="12" y1="20" y2="4"/><line x1="6" x2="6" y1="20" y2="14"/></svg></div>
          <h3 class="svc-title">Consultoría</h3>
          <p class="svc-desc">Asesoría técnica especializada en gestión, supervisión y expedientes técnicos</p>
          <span class="svc-arrow">Ver más <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg></span>
        </div>
      </a>
    </div>
  </section>

  <!-- ③ NOSOTROS -->
  <section class="sec sec--white">
    <div class="container">
      <div class="about-grid">

        <!-- Imagen con badge de años -->
        <div data-aos="fade-right" data-aos-duration="800">
          <div class="about-img-frame">
            <img src="/assets/imagenes/image_nosotros.webp" alt="Equipo INACONS en obra" loading="lazy" width="640" height="540">
            <div class="about-years-badge">
              <span class="badge-num">15+</span>
              <span class="badge-text">Años de<br>experiencia</span>
            </div>
          </div>
        </div>

        <!-- Contenido -->
        <div class="about-content" data-aos="fade-left" data-aos-duration="800">
          <p class="sh-label">Quiénes somos</p>
          <h2 class="about-heading">Construimos los proyectos que mueven al Perú</h2>
          <div class="about-divider"></div>
          <p class="about-body-text">Somos una empresa peruana de ingeniería y construcción con más de 15 años ejecutando proyectos en toda la geografía nacional — desde la costa del Pacífico hasta la alta sierra.</p>
          <p class="about-body-text">Trabajamos con empresas líderes del sector público y privado, aplicando las más altas normas de calidad, seguridad y metodología BIM en cada obra.</p>
          <div class="about-kpi-row">
            <div class="about-kpi">
              <span class="about-kpi-num">96+</span>
              <span class="about-kpi-label">Proyectos ejecutados</span>
            </div>
            <div class="about-kpi">
              <span class="about-kpi-num">40+</span>
              <span class="about-kpi-label">Clientes satisfechos</span>
            </div>
            <div class="about-kpi">
              <span class="about-kpi-num">7</span>
              <span class="about-kpi-label">Departamentos</span>
            </div>
          </div>
          <a href="/nosotros" class="btn btn-primary">
            Conoce nuestra historia <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
          </a>
        </div>

      </div>
    </div>
  </section>

  <!-- ④ COBERTURA -->
  <section class="sec sec--muted">
    <div class="container">
      <div class="sh sh--center sh--medium" data-aos="fade-up" data-aos-duration="700">
        <p class="sh-label">Presencia Nacional</p>
        <h2 class="sh-title">Donde construimos</h2>
        <p class="sh-desc">Desde la costa hasta la sierra, operamos en las zonas de mayor actividad económica del país.</p>
      </div>

      <div class="coverage-stats" data-aos="fade-up" data-aos-duration="600">
        <div class="cov-stat">
          <span class="cov-stat-num" data-target="7">0</span>
          <span class="cov-stat-label">Ciudades</span>
        </div>
        <div class="cov-stat">
          <span class="cov-stat-num" data-target="96" data-suffix="+">0</span>
          <span class="cov-stat-label">Proyectos ejecutados</span>
        </div>
        <div class="cov-stat">
          <span class="cov-stat-num" data-target="6">0</span>
          <span class="cov-stat-label">Departamentos</span>
        </div>
      </div>

      <div class="coverage-grid">
        <!-- Mapa SVG interactivo con departamentos -->
        <div class="map-wrap" data-aos="fade-right" data-aos-duration="800">
          <div id="peruMapContainer" aria-label="Mapa de cobertura INACONS en el Perú"></div>
        </div>

        <!-- Panel de estadísticas -->
        <div class="map-stats" data-aos="fade-left" data-aos-duration="800">
          <p class="map-stats-title">Proyectos por ciudad</p>
          <div class="map-stat">
            <div class="map-stat-meta">
              <span class="map-stat-city">Huancayo</span>
              <span class="map-stat-count">50+</span>
            </div>
            <div class="map-stat-bar"><div class="map-stat-fill" data-bar-width="100"></div></div>
          </div>
          <div class="map-stat">
            <div class="map-stat-meta">
              <span class="map-stat-city">Lima</span>
              <span class="map-stat-count">25+</span>
            </div>
            <div class="map-stat-bar"><div class="map-stat-fill" data-bar-width="50"></div></div>
          </div>
          <div class="map-stat">
            <div class="map-stat-meta">
              <span class="map-stat-city">Pasco</span>
              <span class="map-stat-count">6+</span>
            </div>
            <div class="map-stat-bar"><div class="map-stat-fill" data-bar-width="12"></div></div>
          </div>
          <div class="map-stat">
            <div class="map-stat-meta">
              <span class="map-stat-city">Huancavelica</span>
              <span class="map-stat-count">5+</span>
            </div>
            <div class="map-stat-bar"><div class="map-stat-fill" data-bar-width="10"></div></div>
          </div>
          <div class="map-stat">
            <div class="map-stat-meta">
              <span class="map-stat-city">Ica</span>
              <span class="map-stat-count">5+</span>
            </div>
            <div class="map-stat-bar"><div class="map-stat-fill" data-bar-width="10"></div></div>
          </div>
          <div class="map-stat">
            <div class="map-stat-meta">
              <span class="map-stat-city">Piura</span>
              <span class="map-stat-count">3+</span>
            </div>
            <div class="map-stat-bar"><div class="map-stat-fill" data-bar-width="6"></div></div>
          </div>
          <div class="map-stat">
            <div class="map-stat-meta">
              <span class="map-stat-city">Trujillo</span>
              <span class="map-stat-count">2+</span>
            </div>
            <div class="map-stat-bar"><div class="map-stat-fill" data-bar-width="4"></div></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ⑤ PROYECTOS DESTACADOS — dinámico desde src/content/proyectos/ -->
  {destacados.length > 0 && (
    <section class="projects-showcase">
      <div class="container">
        <div class="sh sh--medium" data-aos="fade-up" data-aos-duration="700">
          <p class="sh-label">Proyectos destacados</p>
          <h2 class="sh-title">Nuestra huella en el Perú</h2>
        </div>
      </div>

      <div
        class="projects-grid"
        style={`grid-template-columns: repeat(${Math.min(destacados.length, 3)}, 1fr)`}
        data-aos="fade-up" data-aos-duration="800" data-aos-delay="100"
      >
        {destacados.map((proyecto) => (
          <a href={`/proyectos/${proyecto.id}`} class="project-card">
            <img
              src={proyecto.data.imagen}
              alt={proyecto.data.titulo}
              loading="lazy"
              width="640"
              height="500"
            />
            <div class="project-overlay"></div>
            <div class="project-info">
              <span class="project-tag">{proyecto.data.categoria}</span>
              <h3 class="project-name">{proyecto.data.titulo}</h3>
              <p class="project-location">{proyecto.data.ubicacion} &middot; {proyecto.data.año}</p>
              <span class="project-cta">
                Ver proyecto
                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
              </span>
            </div>
          </a>
        ))}
      </div>

      <div class="container">
        <div class="projects-footer" data-aos="fade-up" data-aos-duration="600">
          <a href="/proyectos" class="btn btn-outline-light">
            Ver todos los proyectos
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
          </a>
        </div>
      </div>
    </section>
  )}

  <!-- ⑥ CLIENTES -->
  <section class="sec sec--white" id="clientes">
    <div class="container">
      <div class="sh sh--center sh--medium" data-aos="fade-up" data-aos-duration="600">
        <p class="sh-label">Nuestros Clientes</p>
        <h2 class="sh-title">Empresas que confían en nuestra experiencia y calidad</h2>
        <p class="sh-desc">Trabajamos con empresas líderes que valoran la calidad, innovación y compromiso en cada
          proyecto de construcción.</p>
      </div>
      <div class="clients-track-wrap">
        <div class="clients-track">
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_electrocentro.png" alt="Electrocentro" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_electrodunas.png" alt="Electrodunas" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_enel.png" alt="Enel" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_epsgrau.png" alt="EPS Grau" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_grupocentenario.png" alt="Grupo Centenario" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_indecopi.png" alt="Indecopi" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_losportales.png" alt="Los Portales" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_luzdelsur.png" alt="Luz del Sur" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_mediacommerce.png" alt="Media Commerce" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_sanmiguel.png" alt="San Miguel" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_sedapal.png" alt="Sedapal" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_zv.png" alt="ZV" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/ge_vernova.svg" alt="GE Vernova" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/kallpa.png" alt="Kallpa" loading="lazy"></div>
          <!-- Duplicado para scroll infinito -->
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_electrocentro.png" alt="Electrocentro" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_electrodunas.png" alt="Electrodunas" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_enel.png" alt="Enel" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_epsgrau.png" alt="EPS Grau" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_grupocentenario.png" alt="Grupo Centenario" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_indecopi.png" alt="Indecopi" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_losportales.png" alt="Los Portales" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_luzdelsur.png" alt="Luz del Sur" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_mediacommerce.png" alt="Media Commerce" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_sanmiguel.png" alt="San Miguel" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_sedapal.png" alt="Sedapal" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/cliente_zv.png" alt="ZV" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/ge_vernova.svg" alt="GE Vernova" loading="lazy"></div>
          <div class="client-logo-item"><img src="/assets/imagenes/clientes/kallpa.png" alt="Kallpa" loading="lazy"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ⑥ CTA -->
  <section class="cta-band" id="quick-contact">
    <div class="container">
      <div class="sh sh--center sh--medium" data-aos="fade-up">
        <p class="sh-label">¿Tienes un proyecto?</p>
        <h2 class="sh-title">¿Listo para iniciar su proyecto?</h2>
        <p class="sh-desc">Nuestro equipo de expertos está listo para asesorarlo en soluciones de ingeniería y construcción a medida.</p>
      </div>
      <div class="cta-actions" data-aos="fade-up">
        <a href="/contacto" class="btn btn-primary btn-lg">
          Contáctenos <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
        <a href="/assets/documentos/brochure_inacons.pdf" target="_blank" rel="noopener noreferrer" class="btn btn-outline-light btn-lg">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" x2="12" y1="15" y2="3"/></svg> Descargar Brochure
        </a>
        <a href="https://wa.me/51948888070" target="_blank" rel="noopener" class="btn btn-outline-light btn-lg">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 0 1-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 0 1-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 0 1 2.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0 0 12.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 0 0 5.683 1.448h.005c6.554 0 11.890-5.335 11.893-11.893a11.821 11.821 0 0 0-3.48-8.413Z"/></svg> WhatsApp
        </a>
      </div>
    </div>
  </section>

  <!-- Swiper inicializado desde /assets/js/main.js con defer -->

</BaseLayout>